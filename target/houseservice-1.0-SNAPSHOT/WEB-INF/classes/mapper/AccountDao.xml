<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--类名-->
<mapper namespace="com.house.dao.AccountDao">
    <!--id:方法名   result：返回值类型-->
    <insert id="insertAccount" parameterType="com.house.entity.Account" useGeneratedKeys="true" keyColumn="account_id" keyProperty="accountid">
      insert into
      tb_account(username,password,account_type)
      values(#{username},#{password},#{accounttype})
    </insert>

    <update id="updateAccount" parameterType="com.house.entity.Account">
      update tb_account
      set password=#{password}
      where account_id=#{accountid}
    </update>

    <delete id="deleteAccount" parameterType="long">
        delete from tb_account
        where account_id=#{accountid}
    </delete>

    <select id="queryAccount" parameterType="com.house.entity.Account" resultType="com.house.entity.Account">
      select account_id
      from tb_account
      where username=#{username} and password=#{password} and account_type=#{accounttype};
    </select>
</mapper>